import { useEffect } from 'react';

function Home(params) {
  // useEffect(() => {}, []); якщо треба HTTP запитробимо його тут при монтувані сторінки

  return <div>Тут розмітка home 👾 </div>;
}

export default Home;
